# NVIDIA CUDA fine-tuning with Unsloth + TRL
#
# Step 1 — Install PyTorch for your CUDA version (pick the matching line):
#   CUDA 12.4:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#   CUDA 12.1:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11.8:  pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#
# Step 2 — Install training dependencies:
#   pip install -r requirements-train-cuda.txt
#
# Minimum VRAM: ~6 GB (4-bit, 7B model). Recommended: 12 GB+ for batch > 1.

unsloth>=2024.11
trl>=0.12
datasets>=3.0
transformers>=4.45
accelerate>=0.34
bitsandbytes>=0.44
peft>=0.13
anthropic>=0.30         # synthetic data generation via Claude
