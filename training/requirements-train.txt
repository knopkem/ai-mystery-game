# Apple Silicon (MLX) fine-tuning — no CUDA/Triton required.
# For NVIDIA CUDA see requirements-train-cuda.txt
# For AMD ROCm   see requirements-train-rocm.txt
mlx-tune>=0.4
mlx>=0.20
mlx-lm>=0.20
datasets>=3.0
transformers>=4.45   # tokenizer only; no torch training loop needed
anthropic>=0.30        # for synthetic data generation via Claude
