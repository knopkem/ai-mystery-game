# Python
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg-info/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Virtual environments
.venv/
venv/
env/

# Secrets — never commit these
.env
*.env.local

# ── Model weights (large binaries, download separately) ──────────────────────
# Base model + fine-tuned adapters
llm-server/model/
training/model-gguf/

# Training outputs (checkpoints, intermediate saves)
training/checkpoints/
training/outputs*/
training/steps-epoch*/
training/runs/

# NOTE: training/data/ is intentionally tracked (JSONL training datasets)

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Editor
.vscode/
.idea/
*.swp
*.swo
